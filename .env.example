# 拷到「工作根目录」下重命名为 .env（不是放在技能目录里）。.env 已在 .gitignore 中。
# 工作根 = doctor 第 0 节打印的 HOME。

# ── 1. 生成能力（无外部依赖）──
# 已移除 LLM 配置（2026-09-18）：本 skill **不调用任何外部 LLM 接口**。
# 打分 / 打招呼语 / 打分要求 / 简历定制 全部由调用本 skill 的 agent 充当模型，
# 通过 state/agent-llm/requests.json → responses.json 一轮往返完成（见 SKILL.md）。
# 因此不需要 LLM_API_KEY / baseUrl / model。

# ── 2. 发送 ──
# 已移除 DRY-RUN 总闸（2026-09-18）：send/attach 一律真实发送，不再有演练模式、不再要确认。
# 硬门只剩 config/limits.json 的 dailyCap（日上限）与 state/sent.jsonl 去重。
# 想先看看会发什么：只用只读命令（search/score/detail/draft），不跑 send。

# ── 3. 浏览器 ──
CDP_PORT=9222
# 无头模式：现在是**默认**。功能可用，但实测会被猎聘判定为异常行为并跳验证码页。
# 想要有头正常弹窗：HEADLESS=0 或命令行 --headed
# 想要有头但窗口挪到屏幕外（避开风控）：BACKGROUND=1 或 --background
# HEADLESS=0
# 没装 Chrome / 装在别处时指定路径（留空时默认用 CloakBrowser 自带 Chromium，
# 再回退到系统 Google Chrome）
# CHROME_PATH=D:\tools\cloakbrowser\148.0.7778.215\browser\chrome.exe

# ── 4. 站点与路径 ──
# 多站点时切换适配器（见 src/sites/index.mjs）；不填则读 config/site.json
# SITE=liepin
# 个人数据（config/ state/ artifacts/）所在目录。不填则自动往上找。
# JOB_APPLY_HOME=D:\my-job-data
